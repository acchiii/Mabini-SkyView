services:
  - type: web
    name: mabini-skyview
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python seed_data.py
    startCommand: gunicorn mabini_skyview.wsgi:application
    autoDeploy: true